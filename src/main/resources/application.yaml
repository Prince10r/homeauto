spring:
  jpa:
    show-sql: true
    generate-ddl: true
  datasource:
    url: jdbc:mysql://localhost:3306/homeauto
    username: root
    password: Jake!2045
  sql:
    init:
      platform: mysql
      mode: always
